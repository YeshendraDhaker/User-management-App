spring:
  application:
    name: user-management
  datasource:
    url: jdbc:${MYSQL_PUBLIC_URL}
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
